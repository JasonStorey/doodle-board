{
  "name": "doodle-board",
  "version": "0.1.0",
  "description": "",
  "main": "./index.js",
  "scripts": {
    "clean": "mkdir -p ./dist && rm -rf ./dist/*",
    "build": "npm run clean && browserify ./index.js --standalone DOODLE -t babelify -o ./dist/doodle-board.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": {
    "name": "Jason A. Storey",
    "url": "https://github.com/JasonStorey"
  },
  "license": "MIT",
  "devDependencies": {
    "babelify": "^6.1.3",
    "browserify": "^11.0.0"
  }
}
